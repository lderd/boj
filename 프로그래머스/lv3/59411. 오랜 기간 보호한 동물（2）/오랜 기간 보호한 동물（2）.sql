-- 코드를 입력하세요
SELECT ANIMAL_ID, NAME FROM (SELECT AI.ANIMAL_ID, AI.NAME, AO.DATETIME - AI.DATETIME AS GAP FROM ANIMAL_INS AI, ANIMAL_OUTS AO WHERE AI.ANIMAL_ID = AO.ANIMAL_ID ORDER BY GAP DESC) WHERE ROWNUM < 3;